# Synapse v0.1.1

Release date: 2026-04-03

This release closes the first stabilization pass after `v0.1.0`:
- completes existing-memory adoption controls (`adoption_mode`, source-ownership policies, shadow retrieval checks);
- tightens OSS publish transparency (explicit registry status in docs, stricter publish-hygiene checks);
- hardens release metadata consistency across Python + npm packages.

## Package Versions

- PyPI: `synapseworkspace-sdk==0.1.1`
- npm: `@synapseworkspace/sdk@0.1.1`
- npm: `@synapseworkspace/schema@0.1.1`
- npm: `@synapseworkspace/openclaw-plugin@0.1.1`

## Validation Snapshot

Before tag push:
- `python3 scripts/check_release_versions.py`
- `python3 scripts/check_publish_hygiene.py`
- `SYNAPSE_SKIP_WEB_E2E=1 ./scripts/ci_checks.sh`

## Notes

- If package registries still return `not found`, verify `release-packages` workflow for tag `v0.1.1` and confirm trusted publishing + `RELEASE_PUBLISH_ENABLED=true`.
